🕵️‍♂️ ماموریت: تست نفوذ و تضمین کیفیت (QA & Penetration Test)
پروژه: Cost-Control System
سطح تست: Deep Dive (منطقی، امنیتی، دیتابیسی، UI/UX)

📦 بخش ۱: مدیریت کالا (Inventory Item Management)
هدف: اطمینان از اینکه خشت اول (تعریف کالا) کج گذاشته نمی‌شود.

TC-101: ساخت کالا با دیتای صحیح

ورودی: نام="کالای تست ۱"، واحد="کیلوگرم"، دسته="Food"، قیمت=۱۰۰۰.

انتظار: کالا با id جدید در دیتابیس ساخته شود. current_stock پیش‌فرض باید ۰ باشد.

TC-102: ساخت کالا با کد تکراری

ورودی: همان کد کالای قبلی (مثلاً F001).

انتظار: ارور "کد کالا تکراری است".

TC-103: ساخت کالا با نام فارسی/انگلیسی ترکیبی

ورودی: نام="Rice برنج 10kg".

انتظار: ذخیره صحیح یونیکد (UTF-8) در دیتابیس بدون کاراکترهای عجیب (؟).

TC-104: ویرایش کالا و تغییر واحد

ورودی: تغییر واحد از "کیلوگرم" به "عدد".

انتظار: سیستم باید هشدار دهد یا منطق تبدیل واحد را چک کند (آیا conversion_factor نال می‌شود؟).

TC-105: غیرفعال کردن کالا (Soft Delete)

ورودی: تیک is_active را بردارید.

انتظار: کالا در لیست "ثبت تراکنش جدید" نمایش داده نشود، اما در گزارشات قدیمی باقی بماند.

💳 بخش ۲: موتور تراکنش‌ها (Transaction Core Logic)
هدف: پول و موجودی نباید یک ریال جابجا شود.

TC-201: خرید عادی

اکشن: خرید ۱۰ عدد، قیمت ۱۰۰۰.

انتظار: موجودی += ۱۰. میانگین قیمت (اگر FIFO/AVG دارید) آپدیت شود.

TC-202: مصرف عادی

اکشن: مصرف ۵ عدد.

انتظار: موجودی -= ۵.

TC-203: مصرف بیش از موجودی

اکشن: موجودی فعلی ۵، درخواست مصرف ۱۰.

انتظار: بلاک شدن تراکنش با ارور "موجودی کافی نیست".

TC-204: ضایعات با دلیل

اکشن: ضایعات ۱ عدد، دلیل="تاریخ انقضا".

انتظار: ثبت موفق، کسر از موجودی.

TC-205: ضایعات بدون دلیل

اکشن: انتخاب نوع "ضایعات"، خالی گذاشتن فیلد "دلیل".

انتظار: ارور "دلیل ضایعات الزامی است".

TC-206: باگ اعشاری (Floating Point)

اکشن: خرید ۰.۱ + ۰.۲ کیلوگرم.

انتظار: موجودی باید دقیقاً 0.300 باشد (نه 0.3000000004).

TC-207: قیمت صفر

اکشن: خرید با قیمت ۰ (هدایا/Sample).

انتظار: ثبت شود (مجاز است)، اما میانگین قیمت خرید کل را پایین بیاورد.

TC-208: قیمت منفی

اکشن: قیمت = -۱۰۰۰.

انتظار: ارور Validation.

🚨 بخش ۳: سیستم هشدار و هوشمندی (Alerts & Logic)
هدف: تست مغز سیستم.

TC-301: تریگر حداقل موجودی

پیش‌شرط: min_stock = ۱۰.

اکشن: مصرف کردن کالا تا موجودی برسد به ۹.

انتظار: ایجاد رکورد جدید در جدول Alerts.

TC-302: جلوگیری از هشدار تکراری

اکشن: دوباره ۱ عدد مصرف کنید (موجودی ۸).

انتظار: نباید هشدار جدید ساخته شود (همان هشدار قبلی آپدیت شود یا بماند).

TC-303: رفع هشدار (Auto-Resolve)

اکشن: خرید ۲۰ عدد (موجودی ۲۸ > ۱۰).

انتظار: وضعیت هشدار قبلی باید resolved شود.

TC-304: هشدار تایید ضایعات (Approval)

اکشن: ثبت ضایعات با مبلغ کل ۱۰۰ میلیون تومان.

انتظار: تراکنش ثبت شود اما با وضعیت pending_approval. موجودی کسر نشود (یا بشود ولی منتظر تایید بماند - بسته به منطق سیستم).

🛡️ بخش ۴: امنیت و دسترسی (Security & Perms)
هدف: هک کردن سیستم.

TC-401: دسترسی بدون لاگین

اکشن: باز کردن URL /transactions/create در مرورگر ناشناس (Incognito).

انتظار: ریدارکت به صفحه لاگین.

TC-402: دسترسی کارمند به صفحه ادمین

اکشن: لاگین با یوزر staff، تلاش برای باز کردن /admin/users.

انتظار: ارور ۴۰۳ (Access Denied).

TC-403: تغییر قیمت بدون مجوز

اکشن: یوزر staff تلاش کند قیمت خرید را تغییر دهد.

انتظار: ارور "نیاز به تایید مدیر".

TC-404: تزریق اسکریپت (XSS)

ورودی: در توضیحات تراکنش بنویسید <img src=x onerror=alert(1)>.

انتظار: متن باید به صورت Safe Text نمایش داده شود، پنجره Alert باز نشود.

TC-405: نشت اطلاعات هتل دیگر

اکشن: تغییر hotel_id در URL یا فرم (IDOR Attack).

انتظار: ارور ۴۰۳ یا ۴۰۴ (نباید دیتای هتل دیگر را ببیند).

📥 بخش ۵: ورودی و خروجی داده (Import/Export)
هدف: تست پایداری در حجم بالا.

TC-501: ایمپورت اکسل استاندارد

اکشن: آپلود فایل اکسل سالم با ۱۰۰ ردیف.

انتظار: ۱۰۰ کالا اضافه شود.

TC-502: ایمپورت با ستون‌های جابجا

اکشن: جای ستون "قیمت" و "تعداد" را در اکسل عوض کنید.

انتظار: سیستم باید هوشمندانه ستون‌ها را بر اساس Header تشخیص دهد یا ارور شفاف بدهد.

TC-503: ایمپورت فایل حجیم (Stress Test)

اکشن: آپلود اکسل با ۵۰۰۰ ردیف.

انتظار: تایم‌اوت نخورد (یا پروسه پس‌زمینه اجرا شود).

TC-504: ایمپورت فایل غیر اکسل

اکشن: آپلود فایل .exe یا .jpg.

انتظار: ارور "فرمت فایل نامعتبر است".

TC-505: تمیزکاری فایل آپلودی

اکشن: بعد از ایمپورت (چه موفق چه ناموفق).

انتظار: فایل از پوشه uploads سرور حذف شده باشد.

📊 بخش ۶: گزارشات و داشبورد (Reporting)
هدف: صحت اعداد مدیریتی.

TC-601: محاسبه جمع کل امروز

اکشن: ۳ خرید با مبالغ ۱۰۰، ۲۰۰، ۳۰۰ ثبت کنید.

انتظار: ویجت "خرید امروز" در داشبورد دقیقاً 600 را نشان دهد.

TC-602: فیلتر تاریخ (Date Range)

اکشن: فیلتر گزارش از "فردا" تا "پس‌فردا".

انتظار: لیست خالی (نباید دیتای امروز را نشان دهد).

TC-603: باگ منطقه زمانی (Timezone)

اکشن: ثبت تراکنش در ساعت ۲۳:۳۰ شب.

انتظار: باید جزو آمار "امروز" باشد، نه "فردا" (و برعکس برای ساعت ۰۰:۳۰ بامداد).

⚙️ بخش ۷: تنظیمات و پیکربندی (Settings)
TC-701: تغییر پسورد

اکشن: تغییر پسورد ادمین.

انتظار: لاگین با پسورد قدیم ناموفق، با پسورد جدید موفق.

TC-702: قفل شدن حساب (Brute Force)

اکشن: ۵ بار پسورد اشتباه بزنید.

انتظار: حساب کاربری برای ۳ دقیقه قفل شود.