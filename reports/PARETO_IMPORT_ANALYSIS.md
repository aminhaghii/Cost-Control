   # گزارش تحلیل پارتو - فیلدهای مورد نیاز و وضعیت Import Data

## خلاصه اجرایی

برای تحلیل پارتو دقیق، سیستم به **تراکنش‌های خرید** با فیلدهای خاص نیاز دارد. DataImporter فعلی فقط Items و موجودی اولیه را وارد می‌کند، نه تراکنش‌های تاریخی.

---

## ۱. فیلدهای مورد نیاز برای تحلیل پارتو

### از مدل Transaction (بر اساس ParetoService):
```python
# فیلدهای ضروری:
- item_code: کد یکتای کالا
- item_name_fa: نام فارسی کالا
- transaction_type: نوع تراکنش (باید 'خرید' باشد)
- category: گروه کالا ('Food' یا 'NonFood')
- total_amount: مبلغ کل (quantity * unit_price)
- transaction_date: تاریخ تراکنش برای فیلتر دوره (مثلاً ۳۰ روز)
- is_deleted: False (حذف نشده)
- is_opening_balance: False (موجودی اولیه نباشد)
```

### فیلدهای پیشنهادی برای Excel Template:
| ستون | توضیحات | نمونه | ضروری؟ |
|------|---------|-------|--------|
| کد کالا (item_code) | شناسه یکتای کالا | F1234 | ✅ بله |
| نام کالا (item_name_fa) | نام فارسی | برنج | ✅ بله |
| نوع تراکنش (transaction_type) | نوع تراکنش | خرید | ✅ بله |
| گروه (category) | Food یا NonFood | Food | ✅ بله |
| مقدار (quantity) | مقدار خرید | 100 | ✅ بله |
| واحد (unit) | واحد اندازه‌گیری | کیلوگرم | ✅ بله |
| قیمت واحد (unit_price) | قیمت به ریال | 50000 | ✅ بله |
| مبلغ کل (total_amount) | quantity × unit_price | 5000000 | ✅ بله |
| تاریخ (transaction_date) | YYYY-MM-DD | 2026-01-15 | ✅ بله |
| توضیحات (description) | توضیحات اختیاری | خرید از تأمین‌کننده | ❌ خیر |

---

## ۲. وضعیت فعلی DataImporter

### ✅ چه کاری انجام می‌دهد:
- آپلود فایل Excel (حداکثر 16MB)
- شناسایی خودکار ستون‌ها (نام کالا، واحد، موجودی، انبار)
- ایجاد یا به‌روزرسانی Items
- ایجاد تراکنش‌های موجودی اولیه (Opening Balance)
- پشتیبانی از چند شیت و چند هتل

### ⚠️ محدودیت برای تحلیل پارتو:
- **فقط موجودی اولیه و Items را وارد می‌کند**
- **تراکنش‌های تاریخی (خرید/مصرف/ضایعات) را وارد نمی‌کند**
- برای تحلیل پارتو نیاز به داده‌های ۳۰ روزه خرید داریم

### 🐛 مشکل فعلی در تست:
```
عدم تطابق واحد برای کالا 'روغن': سیستم '463937' دارد، فایل '296395' دارد
```
DataImporter ستون 'مبلغ کل (ریال)' را به‌عنوان موجودی (stock) تفسیر می‌کند، نه مبلغ تراکنش.

---

## ۳. راه‌حل پیشنهادی

### گزینه ۱: استفاده از اسکریپت داده‌سازی (پیشنهادی)
```bash
python scripts/populate_test_data.py
```

این اسکریپت:
- ✅ ۱۰,۰۹۱ تراکنش تاریخی در ۳۶۵ روز ایجاد می‌کند
- ✅ همه انواع تراکنش را پوشش می‌دهد (خرید، مصرف، ضایعات، اصلاحی)
- ✅ تحلیل پارتو و ABC را کامل می‌کند
- ✅ فیلدهای مورد نیاز را به‌طور خودکار پر می‌کند

### گزینه ۲: گسترش DataImporter برای تراکنش‌ها
اگر می‌خواهید از فایل Excel خودتان تراکنش‌ها را وارد کنید، باید:

1. **قالب Excel برای تراکنش‌ها:**
   - ستون‌های لازم: `کد کالا`، `نوع تراکنش`، `مقدار`، `قیمت واحد`، `تاریخ`، `دلیل` (برای ضایعات)

2. **تغییرات مورد نیاز در کد:**
   - گسترش `DataImporter` برای پشتیبانی از تراکنش‌ها
   - افزودن route جدید `/admin/import/transactions`
   - اعتبارسنجی و تبدیل داده‌ها

---

## ۴. فایل تست ایجاد شده

### فایل: `pareto_test_data.xlsx`
- ✅ ۱۰۰ ردیف تراکنش خرید
- ✅ فیلدهای کامل: کد کالا، نام، نوع، گروه، مقدار، قیمت، تاریخ
- ✅ توزیع پارتو: ۱۶ قلم کلاس A، ۳۶ قلم B، ۴۸ قلم C
- ✅ مجموع مبلغ: ۸۷۴,۷۳۳,۶۶۶ ریال

### ساختار فایل:
```
Sheet 1: Purchase Transactions (۱۰۰ ردیف)
  - ردیف، کد کالا، نام کالا، نوع تراکنش، گروه، واحد، مقدار، قیمت واحد، مبلغ کل، تاریخ، توضیحات، درصد سهم، مبلغ تجمعی، درصد تجمعی، کلاس

Sheet 2: Summary
  - آمار کلی: تعداد، مبلغ، توزیع کلاس‌ها

Sheet 3: Instructions
  - راهنمای استفاده و فیلدهای مورد نیاز
```

---

## ۵. نتیجه تست DataImporter

### ✅ موفقیت‌ها:
- فایل Excel با موفقیت ایجاد شد
- ساختار فایل صحیح است
- فیلدهای مورد نیاز وجود دارد

### ❌ محدودیت‌ها:
- DataImporter فعلی برای Items و موجودی اولیه طراحی شده
- تراکنش‌های خرید را وارد نمی‌کند
- ستون 'مبلغ کل' به‌عنوان موجودی تفسیر می‌شود (خطا)

### 📊 نتیجه:
```
Import success: True
Total items: 18
Total transactions: 0
Errors: ["عدم تطابق واحد برای کالا 'روغن': سیستم '463937' دارد، فایل '296395' دارد"]
```

---

## ۶. پیشنهاد نهایی

### برای تست فوری تحلیل پارتو:
```bash
python scripts/populate_test_data.py
```

### برای Import از Excel:
نیاز به گسترش DataImporter برای پشتیبانی از تراکنش‌های خرید است. این شامل موارد زیر است:

1. **افزودن قابلیت تشخیص نوع داده:**
   - اگر فایل شامل ستون 'نوع تراکنش' باشد → تراکنش‌ها را وارد کن
   - اگر فقط ستون 'موجودی' باشد → Items و موجودی اولیه را وارد کن

2. **ایجاد تراکنش‌های خرید:**
   ```python
   transaction = Transaction.create_transaction(
       item_id=item.id,
       transaction_type='خرید',
       quantity=row['مقدار'],
       unit_price=row['قیمت واحد'],
       category=row['گروه'],
       hotel_id=self.hotel_id,
       user_id=self.user_id,
       transaction_date=row['تاریخ'],
       description=row.get('توضیحات', '')
   )
   db.session.add(transaction)
   ```

3. **اعتبارسنجی:**
   - بررسی وجود Item برای هر کد کالا
   - اعتبارسنجی مقدار و قیمت
   - تبدیل تاریخ به فرمت Date

---

## ۷. فایل‌های مرجع

- `services/pareto_service.py` - منطق تحلیل پارتو
- `services/data_importer.py` - سرویس Import فعلی
- `scripts/create_pareto_test_excel.py` - ایجاد فایل تست
- `pareto_test_data.xlsx` - فایل Excel نمونه (۱۰۰ ردیف)

---

## ۸. نتیجه‌گیری

✅ **فیلدهای مورد نیاز شناسایی شدند**
✅ **فایل Excel تست ایجاد شد**
❌ **DataImporter فعلی از تراکنش‌های خرید پشتیبانی نمی‌کند**

**پیشنهاد:** استفاده از `populate_test_data.py` برای تست فوری یا گسترش DataImporter برای Import از Excel.
