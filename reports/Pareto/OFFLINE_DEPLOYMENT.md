# راهنمای استقرار کامل آفلاین (100% Offline)
# Complete Offline Deployment Guide

## خلاصه اجرایی
این سیستم مدیریت موجودی هتل اکنون **100% آفلاین** کار می‌کند (به جز قابلیت‌های AI).

---

## ✅ قابلیت‌های آفلاین (بدون نیاز به اینترنت)

### عملکرد کامل آفلاین:
- ✅ **داشبورد کامل** با تمام نمودارها و آمار
- ✅ **مدیریت تراکنش‌ها** (خرید، مصرف، ضایعات، انتقال)
- ✅ **تحلیل پارتو** با نمودارهای تعاملی
- ✅ **طبقه‌بندی ABC** کامل
- ✅ **گزارش‌گیری** و خروجی Excel
- ✅ **مدیریت انبار** و موجودی
- ✅ **تحلیل ضایعات** با نمودارهای روند
- ✅ **مدیریت کاربران** و سطوح دسترسی
- ✅ **چند هتلی** با جداسازی داده
- ✅ **امنیت کامل** (CSRF, Session, Password Hashing)

### ⚠️ نیازمند اینترنت (فقط API):
- API تحلیل هوشمند AI (استفاده از GROQ API)
- چت‌بات هوشمند (استفاده از GROQ API)

**نکته مهم:** حتی بدون API، سیستم با پاسخ‌های fallback کار می‌کند و خرابی ندارد.

---

## 📦 فایل‌های محلی دانلود شده

تمام وابستگی‌های خارجی (CDN) به صورت محلی ذخیره شده‌اند:

### CSS (383 KB):
- `static/css/bootstrap.rtl.min.css` (233 KB) - Bootstrap 5 RTL
- `static/css/font-awesome.min.css` (102 KB) - Font Awesome 6.5.1
- `static/css/select2.min.css` (16 KB) - Select2 dropdown
- `static/css/select2-bootstrap-5-theme.rtl.min.css` (31 KB)
- `static/css/vazirmatn.css` (339 bytes) - فونت فارسی

### JavaScript (446 KB):
- `static/js/jquery-3.7.1.min.js` (87 KB)
- `static/js/bootstrap.bundle.min.js` (80 KB)
- `static/js/select2.min.js` (73 KB)
- `static/js/chart.umd.min.js` (205 KB) - نمودارها

### فونت‌ها (401 KB):
- `static/webfonts/fa-solid-900.woff2` (156 KB) - آیکون‌های Font Awesome
- `static/webfonts/fa-regular-400.woff2` (25 KB)
- `static/webfonts/fa-brands-400.woff2` (117 KB)
- `static/fonts/Vazirmatn-Regular.woff2` (50 KB) - فونت وزیرمتن
- `static/fonts/Vazirmatn-Bold.woff2` (51 KB)

**مجموع:** 1.2 مگابایت فایل‌های استاتیک

---

## 🚀 نصب در سیستم جدید (کاملاً آفلاین)

### گام 1: کپی پروژه
```bash
# کل پوشه pareto را کپی کنید
# همه فایل‌های static/ باید کپی شوند
```

### گام 2: نصب Python و وابستگی‌ها
```bash
# Python 3.8+ نصب کنید (یکبار، آفلاین با installer)
# در سیستم جدید:
cd pareto
python -m venv .venv
.venv\Scripts\activate  # Windows
pip install -r requirements.txt
```

### گام 3: پیکربندی
```bash
# فایل .env بسازید
SECRET_KEY=your-secret-key-here
GROQ_API_KEY=  # خالی بگذارید برای آفلاین
```

### گام 4: راه‌اندازی دیتابیس
```bash
python init_db.py
```

### گام 5: اجرا
```bash
python app.py
```

**آدرس:** http://localhost:8084

**کاربر پیش‌فرض:**
- نام کاربری: `admin`
- رمز عبور: `Admin@1234`

---

## 🔧 تنظیمات آفلاین

### غیرفعال کردن کامل AI (اختیاری):
در فایل `.env`:
```
GROQ_API_KEY=
```

سیستم به صورت خودکار به حالت fallback می‌رود و بدون خطا کار می‌کند.

### تنظیمات امنیتی:
```python
# config.py
SESSION_COOKIE_SECURE = False  # برای HTTP محلی
WTF_CSRF_ENABLED = True  # همیشه فعال
```

---

## 📊 عملکرد در حالت آفلاین

### سرویس‌های آفلاین:
1. **ParetoService**: تحلیل 80/20 با pandas
2. **ABCService**: طبقه‌بندی سه‌سطحی
3. **WarehouseService**: مدیریت موجودی و انبار
4. **WasteAnalysisService**: تحلیل ضایعات
5. **ExportService**: خروجی Excel
6. **AuthService**: احراز هویت و مجوزها

### سرویس‌های AI (با fallback):
- `WorkflowAnalyzer`: بدون API، پاسخ‌های از پیش تعریف شده
- `ChatService`: بدون API، پاسخ‌های پایه

---

## 🎯 چک‌لیست استقرار

- [x] تمام فایل‌های static موجود هستند (1.2 MB)
- [x] تمام template ها از فایل‌های محلی استفاده می‌کنند
- [x] هیچ لینک CDN خارجی باقی نمانده
- [x] Database SQLite محلی است
- [x] Session ها در حافظه ذخیره می‌شوند
- [x] فونت‌های فارسی به صورت محلی لود می‌شوند
- [x] نمودارها با Chart.js محلی کار می‌کنند
- [x] آیکون‌ها با Font Awesome محلی نمایش داده می‌شوند

---

## 🧪 تست آفلاین

### تست کامل بدون اینترنت:
```bash
# 1. اینترنت را قطع کنید
# 2. سرور را راه‌اندازی کنید:
python app.py

# 3. مرورگر را باز کنید:
http://localhost:8084

# 4. عملکرد را بررسی کنید:
✓ صفحه لاگین لود شود
✓ داشبورد با نمودارها نمایش داده شود
✓ فونت‌ها و آیکون‌ها صحیح باشند
✓ تراکنش جدید ثبت شود
✓ گزارشات تولید شوند
✓ Excel خروجی بگیرد
```

---

## 📋 فایل‌های کلیدی

### فایل‌های اصلی:
- `app.py` - نقطه ورود برنامه
- `config.py` - تنظیمات
- `init_db.py` - راه‌اندازی دیتابیس
- `requirements.txt` - وابستگی‌های Python

### فولدرهای مهم:
- `static/` - تمام فایل‌های CSS, JS, Fonts (آفلاین)
- `templates/` - قالب‌های HTML
- `models/` - مدل‌های دیتابیس
- `routes/` - مسیرها و کنترلرها
- `services/` - منطق کسب‌وکار
- `database/` - فایل SQLite

---

## 🔐 امنیت

سیستم دارای لایه‌های امنیتی کامل است:
- ✅ CSRF Protection
- ✅ Session Management
- ✅ Password Hashing (werkzeug)
- ✅ SQL Injection Prevention (SQLAlchemy ORM)
- ✅ XSS Prevention (Flask auto-escaping)
- ✅ Security Headers
- ✅ Rate Limiting

همه بدون نیاز به اینترنت!

---

## 📝 توضیحات فنی

### معماری آفلاین:
```
User Browser
    ↓
Local Files (static/)
    ↓
Flask Server (localhost:8084)
    ↓
SQLite Database (local file)
```

هیچ درخواست خارجی به اینترنت ارسال نمی‌شود.

### CSP (Content Security Policy):
```
default-src 'self';
script-src 'self' 'unsafe-inline' 'unsafe-eval';
style-src 'self' 'unsafe-inline';
font-src 'self';
img-src 'self' data: blob:;
connect-src 'self';
```

تمام منابع از `'self'` (همان سرور) لود می‌شوند.

---

## ❓ سوالات متداول

**Q: آیا می‌توانم سیستم را در سیستم بدون اینترنت نصب کنم؟**
A: بله، 100%! فقط Python و requirements.txt را از قبل آماده کنید.

**Q: آیا AI کار می‌کند؟**
A: در حالت آفلاین، AI به حالت fallback می‌رود و پاسخ‌های پایه می‌دهد. برای AI کامل، GROQ_API_KEY لازم است.

**Q: آیا نمودارها کار می‌کنند؟**
A: بله! Chart.js به صورت محلی نصب شده و کاملاً آفلاین کار می‌کند.

**Q: چه کسانی می‌توانند از سیستم استفاده کنند؟**
A: هرکسی که به سرور محلی (localhost یا IP داخلی) دسترسی داشته باشد.

**Q: آیا می‌توانم در شبکه داخلی (LAN) استفاده کنم؟**
A: بله! در app.py، host='0.0.0.0' است، پس تمام دستگاه‌های شبکه محلی دسترسی دارند.

---

## 🎉 نتیجه

سیستم اکنون **100% آفلاین** است و در هر محیطی بدون اینترنت قابل استفاده.

### مزایا:
- ✅ **سرعت بالا** (بدون انتظار برای CDN)
- ✅ **قابل اعتماد** (بدون وابستگی به سرویس‌های خارجی)
- ✅ **امن** (داده‌ها در شبکه داخلی)
- ✅ **کنترل کامل** (هیچ درخواست خارجی)

### محدودیت:
- ⚠️ AI analysis نیازمند GROQ API (اختیاری)
- ⚠️ Chat bot نیازمند GROQ API (اختیاری)

**توجه:** حتی بدون API، سیستم بدون خطا کار می‌کند!

---

## 📞 پشتیبانی

برای مشکلات:
1. بررسی کنید تمام فایل‌های `static/` کپی شده‌اند
2. لاگ‌های `app.log` را بررسی کنید
3. Database با `init_db.py` ساخته شده است

**نسخه:** 2.0 - Fully Offline Edition  
**تاریخ:** 2026-01-26
